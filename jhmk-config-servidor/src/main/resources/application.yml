server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hokjose/micro18.git
          searchPaths: config-data
          default-label: main
  security:
    user:
      name: jose
      password: miriam
